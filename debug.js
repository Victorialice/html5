drawLine(ctx, i * (canvas.height / lines), canvas.height / lines);

drawLine(ctx, 0, 200) 

ctx.fillRect(0, y, ctx.canvas.width, h); //context.fillRect(x,y,width,height);
ctx.fillRect(0, 0, 500, 200);
      
